{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.ts --minify --format=esm --bundle --outdir=src/itables_anywidget/static",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {},
	"devDependencies": {
		"@anywidget/types": "^0.2.0",
		"typescript": "^5.6.2",
		"esbuild": "^0.23.1"
	}
}
